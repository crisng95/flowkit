"""
EXAMPLE ONLY — replace with a real authorized provider adapter.
Never put credentials in this file.
"""
def build_adapter():
    raise RuntimeError(
        "Implement this adapter against the authorized shipping provider test account. "
        "See LIVE_PROVIDER_ADAPTER_CONTRACT.md."
    )
